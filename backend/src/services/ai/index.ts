// active ai provider - to switch providers, just change this import
// e.g. swap geminiProvider for openaiProvider and nothing else changes
import { geminiProvider } from './gemini.provider';

export const aiProvider = geminiProvider;
